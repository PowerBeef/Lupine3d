Design ONE cohesive full-width Game Boy Color FPS HUD for Sable Outpost, inspired by the tactile industrial status bar and information hierarchy of original Doom, using completely original art. The user says the ENTIRE existing HUD looks simplistic and washed out, not just its portrait. Produce a polished design proposal sheet focusing on all HUD elements together. Native target is EXACTLY 160 pixels wide by 24 pixels high; the width to height ratio of each HUD strip MUST be 6.667 to 1. Render the same strip at an integer enlargement for inspection. A wide low continuous steel console, NOT a tall dashboard. No element extends above or below the strip. Portrait stays 16x16 in a 24x24 centre bay with FOUR PIXELS vertical padding, never dominates the HUD. Layout left to right: health plus icon at x6..15 and large cream 6x10 pixel digits '99' at x24..39; centre helmeted pilot portrait at x72..87 y4..19; a deliberately small hostile silhouette at x104..111 and a SINGLE smaller 5x7 digit '1' at x113; small 'EXIT' at x128 above compact 'LOCK' at x128. Correct overall balance and restrained hierarchy are critical: health the largest number, hostile count visibly smaller. Whole panel visual treatment: continuous one-pixel top divider; restrained two-tone steel edging and small stepped inset corners; darker recesses behind readouts; sparse purposeful pixel notches; readable plus icon, angular small enemy icon, clear consistent compact typography; one shallow portrait recess integrated into the same panel, not three individually framed boxes. Strong value contrast in all elements, no blurry muted badge look. Original pilot with stern readable eyes under angular helmet and small respirator, powerful dark and cream facial clusters. No extra counters, ammo, armour, keys, meters, button hints, symbols without function, gradients, glow or photorealism. Limit actual HUD artwork to four flat colors: near-black #101820, steel #66777A, warm ivory #EEE6C5, restrained desaturated teal #528484. Large simple native-pixel clusters: do not fake tiny detailed art that cannot survive a 160x24 raster. Board on dark background with title 'SABLE OUTPOST — COMPACT STEEL HUD'. Show the complete default strip enlarged, then two smaller full-width examples preserving the EXACT same proportions and layout: hurt health '42', hostile '1', EXIT LOCK; and cleared state health '79', hostile '0', EXIT OPEN. Use short external callouts pointing to the panel material, health hierarchy, portrait contrast and status typography. This is a CONCEPT BOARD, not an actual ROM capture. Focus on a coherent treatment of the WHOLE HUD, with real empty breathing space inside its fixed 24-pixel height.
